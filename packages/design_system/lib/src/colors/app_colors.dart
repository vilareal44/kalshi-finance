import 'package:flutter/material.dart';

/// Defines the color palette for the App UI Kit.
class AppColors {
  /// Primary color for the app.
  static const Color primary = Color.fromRGBO(0, 28, 149, 1);

  /// Background color for the app.
  static const Color background = Color.fromRGBO(244, 248, 250, 1);

  /// White color for the app.
  static const Color white = Color.fromRGBO(255, 255, 255, 1);

  /// Dark Gray color for the app.
  static const Color darkGray = Color.fromRGBO(30, 42, 50, 1);

  /// Light Gray color for the app.
  static const Color lightGray = Color.fromRGBO(112, 135, 151, 1);

  /// Medium Gray color for the app.
  static const Color mediumGray = Color.fromRGBO(77, 100, 117, 1);

  /// Extra Light Gray color for the app.
  static const Color extraLightGray = Color.fromRGBO(233, 238, 242, 1);

  /// Shadow color for the app.
  static const Color shadow = Color.fromRGBO(30, 42, 50, 0.08);

  /// Success color for the app.
  static const Color green = Color.fromRGBO(4, 199, 97, 1);

  /// Warning color for the app.
  static const Color yellow = Color.fromRGBO(255, 192, 50, 1);

  /// Error color for the app.
  static const Color red = Color.fromRGBO(211, 42, 54, 1);

  ///
  static const Color lightBlue = Color.fromRGBO(204, 213, 255, 1);
}
